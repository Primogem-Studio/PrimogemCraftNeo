# PrimogemCraftNeo

Before changing this repository, read [.agents/skills/primogemcraft-standard/SKILL.md](.agents/skills/primogemcraft-standard/SKILL.md). Follow its routing table to read only the domain skills relevant to the task. These are ordinary Markdown files; no skill loader, plugin, or particular editor is required.

Keep replies concise and implementation complete. Use `var` for every Java local declaration that can legally preserve the intended semantics, including existing declarations encountered during the task; see the standard for the exceptions. Resolve build tools from the current checkout and environment, not a previous machine's paths.
